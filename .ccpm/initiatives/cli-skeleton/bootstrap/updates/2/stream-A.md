---
issue: 2
stream: bootstrap
started: 2026-05-03T14:18:44Z
status: completed
---
## Scope
- pyproject.toml + uv.lock + hatchling build
- ebless/ package (cli.py, indexer.py, __init__.py)
- tests/test_cli.py
- README.md install/usage refresh

## Progress
- pyproject.toml created (PEP 621, hatchling, click runtime, pytest dev, ebless entry point)
- ebless/ package scaffolded (__init__.py, indexer.py stub, cli.py with Click group + index subcommand)
- tests/test_cli.py created (4 tests: help lists index, happy path, missing path, stub contract via capsys)
- README.md Setup/Install + Usage sections refreshed to use uv
- uv sync succeeded; uv.lock generated
- uv run ebless --help OK (lists index)
- uv run ebless index /tmp/ebless-smoke prints "would index /tmp/ebless-smoke" (exit 0)
- uv run ebless index /does/not/exist exits 2 with Click validation error
- uv run pytest: 4 passed

## Final
Shipped:
- `pyproject.toml` — PEP 621 metadata, click>=8.1 runtime, pytest>=8 dev extra, hatchling backend, `ebless = "ebless.cli:cli"` entry point.
- `uv.lock` — generated by `uv sync`, committed.
- `ebless/__init__.py` — exposes `__version__`.
- `ebless/indexer.py` — `index_books(path: Path) -> None` stub printing `would index <path>`.
- `ebless/cli.py` — Click group `cli` and `index` subcommand validating `path` via `click.Path(exists=True, file_okay=False, dir_okay=True, path_type=pathlib.Path)`.
- `tests/test_cli.py` — four tests (help lists index, CLI happy path, CLI missing-path error, stub contract via `capsys`).
- `README.md` — Setup/Prerequisites/Install rewritten for `uv sync`; Usage commands updated to `uv run ebless …`; Development section added.

All six verification commands pass. Committed and pushed as 143f5d5 on `initiative/cli-skeleton`.
